// Given an array a of integers, return an array b of the same length that has
// in each index the string value of the element in a at the same index

// Example:
// [1,2,3] -> ["1", "2", "3"]

export const itemsToString = (a: number[]): string[] => {
  return [];
};
