/**
 * Given a number n, find the next smallest perfect square
 * (a perfect square is a number that can be expressed as a product of two
 * that is, it is the square of an integer). For example, if n = 13, the next
 * smallest perfect square would be 16.
 */

export const square = (n) => {
    return n;
};