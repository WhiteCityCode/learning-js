// You function takes a string as an argument and returns a boolean value,
// stating if the string is ALL CAPS or not. The string contains only ascii letters.
// Example:
// "Hello" -> false
// "HELLO" -> true

export default function isAllCaps(s) {
  return s;
}
