/**
 * Given a value (needle) and an array (haystack)
 * return `true` if the element is part of the
 * array and `false` otherwise.
 */

export const find = (needle, haystack) => {
  return false;
};
