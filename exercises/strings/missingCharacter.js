// Find the missing letter

// Write a method that takes an string of consecutive letters and returns the
// missing letter.

// You will always get a valid string and it will always be missing exactly one
// letter. The length of the array is always at least 2 and all the letters will
// be the same casing

// Example:
//   "abcdf" -> "e"
//   "OQRS" -> "P"

export const missingCharacter = (letters) => {
  return letters;
};
