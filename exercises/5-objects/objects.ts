/**
 * Your function receives 3 parameters, corresponding to the fields in the
 * person type. Your job is to return an object that respects the `Person`
 * shape.
 *
 * Example: "John" "Doe" 35 -> {firstName: 'John', lastName: 'Doe', age: 35}
 *
 */

type Person = {
  firstName: string;
  lastName: string;
  age: number;
};

export const objects = (
  firstName: string,
  lastName: string,
  age: number
): Person => {
  return unknown;
};
