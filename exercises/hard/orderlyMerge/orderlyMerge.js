/**
 * Take two sorted arrays and return a sorted
 * array containing the elements of both:
 *
 * [1, 2, 4], [3, 5, 6] => [1, 2, 3, 4, 5, 6]
 */

export const orderlyMerge = (arr1, arr2) => {
  return [];
};
