/**
 * Your function takes as an argument an array. Your task is to return
 * all the elements in the array except the first one, or an empty array if there is
 * nothing to return. You are not allowed to use loops or the
 * array.prototype.tail() method.
 *
 * Example: [1,2,3,4] -> [2,3,4]
 *
 *
 */

export const tail = <T>(arr: T[]): T[] => {
  return arr;
};
