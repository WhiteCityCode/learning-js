/**
 * You will receive an array of integers arr. Your task is to return the sum
 * of all the odd elements in the array. You are not allowed to use loops
 * or if.
 *
 *
 * Example: [1,2,3] -> 4
 *          [1,1,1] -> 3
 *          [0,0,1] -> 1
 *          [0,0,0] -> 0
 */

export const sumOdds = (s: number[]): number => {
  return s[0];
};
