/**
 * Given a value (needle) and an array (haystack)
 * return the index of the element if it's part of the
 * array and -1 otherwise.
 */

export const find = (needle: unknown, haystack: unknown[]): number => {
  return -1;
};
