// You are given an array arr of length n. The array contains the numbers from
// 0 up to n, with one number missing. Find the missing number. arr might not
// always be sorted.
// Example:
// [0,1,2,4] -> 3;

export default function missingNumber(arr) {
  return arr;
}
